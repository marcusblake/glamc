func main() int {
    a := [[1,2,3,4,5], [1,2,1,68]];
    
    a[0][len(a[0])-1] = 700;
    a[1] = [101, 102, 103, 104, 105];
    n := len(a);
    i := 0;
    while (i < n) {
        j := 0;
        m := len(a[i]);
        while (j < m) {
            println(a[i][j]);
            j += 1;
        }
        i += 1;
    }
    return 0;
}