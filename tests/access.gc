func main() {
    a := [[1,2,3,4,5], [1,2,1,68]];
    
    a[0][len(a[0])-1] = 700;
    a[1] = [101, 102, 103, 104, 105];
    n := len(a);
    i := 0;

    for (row in a) {
        for (element in row) {
            println(element);
        }
    }
}