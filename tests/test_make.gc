func main() {
    var a list<bool> = make(bool, 10, true);

    count := 10000;

    for (counter in 0...count) {
        append(a, false);
    }

    falseCount := 0;
    trueCount := 0;
    for (id in a) {
        if (id) {
            trueCount += 1;
        } else {
            falseCount += 1;
        }
    }
    
    if (trueCount != 10) {
        println("incorrect number of true in list");
    }

    if (falseCount != count) {
        println("incorrect number of false in list");
    }
}