func is_palindrome(string k) bool {
    j := lenstr(k) - 1;
    i := 0;
    bool is_palindrome = true;
    while (i <= j) {
        if (k[i] != k[j]) {
            is_palindrome = false;
        }
        i = i + 1;
        j = j - 1;
    }
    return is_palindrome;
}


func printStringList(list<string> my_list) int {
    put(my_list, 0, "Start");
    id := lenlist(my_list);
    counter := 0;
    while (counter < id) {
        prints(my_list[counter]);
        counter = counter + 1;
    }
    pop(my_list);
}


func createList(int low, int high) list<int> {
    list<int> temp;
    while (low <= high) {
        append(temp, low);
        low = low + 1;
    }
    return temp;
}


func main() int {
    list<string> words = ["Marcus Blake", "racecar", "dogeeseseegod", "datyayyay"];
    count := 0;
    list<string> palindromes;
    int x;
    bool y;
    printi(x);
    printb(y);
    while (count < 4) {
        str := words[count];
        if (is_palindrome(str)) {
            append(palindromes, str);
        }
        count = count + 1;
    }
    printStringList(palindromes);
    a := createList(1,10);
    count = 0;
    while (count < lenlist(a)) {
        printi(a[count]);
        count = count + 1;
    }
    return 0;
}