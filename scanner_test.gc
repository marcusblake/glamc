int global;
int my_var;

struct Point {
    int x;
    int y;

    func getSum() int {
        return x + y;
    }

    func toString() string {
        return int_to_string(x) + " " + int_to_string(y);
    }
}

func main() int {
   id := 2.22;
   list<int> arr = [1,0.2e-2,3.0e2,4,5];
   Point my_point = Point{x: 1};

   global = my_point.x;
   my_point.x = 2 * 3 + 16 - 4;
   for (var in arr) {
       print(var);
   }

   if ("hello" == "world") {
       println("true");
   } else {
       if (id >= int_to_float(2)) {
           println("greater than two");
       }
   }

   list<list<Point>> my_list = [[Point{x:1, y:2}],[Point{}]];
   return 0;
}