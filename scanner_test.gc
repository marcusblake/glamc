int j;
int l;

func can_you_print(string k) int {
    prints(k);
}

func main() int {
   x := 2;
   y := 14;
   j = 0;
   bool a = true;
   string s = "hello world";
   while (j <= 10) {
       if (j > 2 && j < 7) {
           printi(j);
       }
       j = j + 1;
   }
   d := s;
   can_you_print("Say whats up");
   prints(s);
   return 0;
}