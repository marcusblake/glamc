int global;
int my_var;

struct Point {
    int x;
    int y;

    func getSum() int {
        return x + y;
    }
}

func main() int {
   id := 2;
   list<int> arr = [1,2,3,4,5];
   Point my_point = Point{x: 1};

   global = my_point.x;

   for (var in arr) {
       print(var);
   }

   list<list<Point>> my_list = [[Point{x:1, y:2}],[Point{}]];
   return 0;
}