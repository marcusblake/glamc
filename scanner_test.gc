var a int;
var b int;
var c int;


func main() int {


    d := [[1,2,3,4],[2,4,52,1],[0,1,2,12,94]];



    my_function := func(a: list<int>) int -> {
        sum := 0;
        for (id in a) {
            sum += id;
        } 
        return sum;
    };

    a := map(d, my_function);

    for (id in a) {
        println(id);
    }

    println(len(d));
    println(len("Hello"));
    b := 2.0;
    println(b > 2.0);

    return 0;
}