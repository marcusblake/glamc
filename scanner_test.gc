var a int;
var b int;
var c int;


func main() int {


    d := [[1,2,3,4],[2,4,52,1],[0,1,2,12,94]];

    a := map(d, func(a: list<int>) int -> {
        sum := 0;
        for (id in a) {
            if (id % 2 == 0) {
                sum += 1;
            }
        } 
        return sum; 
    });

    for (id in a) {
        println(id);
    }

    println(len(d));
    println(len("Hello "));

    return 0;
}